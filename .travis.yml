sudo: required
language: go
go:
  - 1.7
before_script:
   - apt-get install -y gcc g++
   - apt-get install -y libgtk-3-0
script:
   - go build
deploy:
  provider: releases
  api_key: "${GH_DEPLOY_KEY}"
  file: "go-md5sum-ui"
  skip_cleanup: true
  on:
    tags: true